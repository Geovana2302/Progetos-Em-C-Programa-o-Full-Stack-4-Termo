<UserControl x:Class="ProjetoVet.Views.AnimalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjetoVet.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="700"
             Background="#FF8BCCEE">
    <UserControl.Resources>
        <SolidColorBrush x:Key="ColorNavy" Color="#000080"/>
        <SolidColorBrush x:Key="ColorLightGray" Color="#F5F5F4"/>
        <SolidColorBrush x:Key="ColorYellow" Color="#FFE676"/>

        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="{StaticResource ColorYellow}"/>
            <Setter Property="Foreground" Value="{StaticResource ColorNavy}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15, 8"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="Label">
            <Setter Property="Foreground" Value="{StaticResource ColorNavy}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0, 10, 0, 5"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="{StaticResource ColorNavy}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource ColorYellow}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="{StaticResource ColorNavy}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource ColorYellow}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="{StaticResource ColorNavy}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource ColorYellow}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Gerenciamento de Animais" 
                   FontSize="24" FontWeight="Bold" 
                   Foreground="{StaticResource ColorNavy}" 
                   Margin="0, 0, 0, 20"/>

        <Expander Grid.Row="1" 
                  x:Name="FormExpander" 
                  Header="[+] Adicionar Novo Animal" 
                  FontSize="16" 
                  FontWeight="SemiBold"
                  Foreground="{StaticResource ColorNavy}"
                  Margin="0, 0, 0, 10"
                  IsExpanded="False">
            <StackPanel>
                <Grid Margin="0, 10, 0, 20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Nome:" Style="{StaticResource Label}"/>
                        <TextBox x:Name="txtNome"/>

                        <TextBlock Text="Sexo:" Style="{StaticResource Label}"/>
                        <TextBox x:Name="txtSexo"/>

                        <TextBlock Text="Data Nasc.:" Style="{StaticResource Label}"/>
                        <DatePicker x:Name="dpDataNascimento"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Tutor:" Style="{StaticResource Label}"/>
                        <ComboBox x:Name="cmbTutor"/>

                        <TextBlock Text="Espécie:" Style="{StaticResource Label}"/>
                        <ComboBox x:Name="cmbEspecie"
                                  SelectionChanged="cmbEspecie_SelectionChanged"/>

                        <TextBlock Text="Raça:" Style="{StaticResource Label}"/>
                        <ComboBox x:Name="cmbRaca" IsEnabled="False"/>
                    </StackPanel>
                </Grid>

                <StackPanel Orientation="Horizontal" Margin="0, 0, 0, 10">
                    <Button x:Name="btnSalvar" 
                            Content="Salvar" 
                            Click="btnSalvar_Click" 
                            Background="Navy" Foreground="White"
                            Style="{StaticResource PrimaryButton}"/>
                    <Button x:Name="btnCancelar" 
                            Content="Cancelar"
                            Margin="10,0,0,0"
                            Background="#FF6A6A78" 
                            Foreground="White"
                            FontWeight="Bold"
                            BorderThickness="0"
                            Padding="15, 8"
                            MinWidth="100"
                            Click="btnCancelar_Click"/>
                </StackPanel>
            </StackPanel>
        </Expander>

        <StackPanel Grid.Row="2" Margin="0, 10, 0, 10">
            <TextBlock Text="Pesquisar por Nome ou Tutor:" Style="{StaticResource Label}"/>
            <TextBox x:Name="txtPesquisa"
                     TextChanged="txtPesquisa_TextChanged"/>
        </StackPanel>

        <DataGrid Grid.Row="3" 
                  x:Name="dataGridAnimais"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding AnimalID}" Width="Auto"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="Tutor" Binding="{Binding Tutor.Nome}" Width="*"/>
                <DataGridTextColumn Header="Espécie" Binding="{Binding Especie.Nome}" Width="Auto"/>
                <DataGridTextColumn Header="Raça" Binding="{Binding Raca.Nome}" Width="Auto"/>

                <DataGridTemplateColumn Header="Editar" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="✏️" Background="Transparent" BorderThickness="0" FontWeight="Bold" FontSize="14" Foreground="#FF4A8DCA" Cursor="Hand"
                                    Click="ButtonEditar_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Excluir" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="🗑️" Background="Transparent" BorderThickness="0" FontWeight="Bold" FontSize="14" Foreground="#FFC13939" Cursor="Hand"
                                    Click="ButtonExcluir_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>